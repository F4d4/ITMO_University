#include "sys_tick.h"
#include "stm32f4xx_hal.h"

/*
 * Реализация миллисекундного счётчика для стенда. Счётчик наращивает
 * обработчик прерывания системного таймера SysTick - единственного таймера,
 * разрешённого заданием.
 */
uint32_t sys_tick_ms(void)
{
    return HAL_GetTick();
}
