# Vehicle Management System - Frontend

React-приложение для управления транспортными средствами.

## 🚀 Технологии

- **React** 18.2.0
- **React Router** 6.20.0
- **Axios** для HTTP запросов
- **CSS3** для стилизации

## 📋 Функциональность

### Основные возможности:
- ✅ Просмотр всех транспортных средств с пагинацией
- ✅ Фильтрация по полям (name, type, fuelType)
- ✅ Сортировка по любому полю (ASC/DESC)
- ✅ Создание нового Vehicle
- ✅ Просмотр детальной информации о Vehicle
- ✅ Редактирование существующего Vehicle
- ✅ Удаление Vehicle
- ✅ Информативные сообщения об ошибках

### Специальные операции:
- 🔍 Найти Vehicle с максимальной вместимостью
- 🔤 Поиск по префиксу имени
- ⛽ Поиск по расходу топлива (больше заданного)
- 🚗 Поиск по типу транспортного средства
- 🔄 Сброс пробега до нуля

## 🛠️ Установка и запуск

### Предварительные требования:
- Node.js 14+ и npm
- Backend должен быть запущен на `http://localhost:8080`

### Установка зависимостей:

```bash
cd frontend
npm install
```

### Запуск в режиме разработки:

```bash
npm start
```

Приложение откроется на `http://localhost:3000`

### Сборка для production:

```bash
npm run build
```

Оптимизированные файлы будут в папке `build/`

## 📁 Структура проекта

```
frontend/
├── public/
│   └── index.html              # HTML шаблон
├── src/
│   ├── components/
│   │   ├── Layout/             # Компонент макета (Header, Nav, Footer)
│   │   ├── Vehicle/            # Компоненты для работы с Vehicle
│   │   │   ├── VehicleList.js      # Список с пагинацией и фильтрацией
│   │   │   ├── VehicleCreate.js    # Форма создания
│   │   │   ├── VehicleEdit.js      # Форма редактирования
│   │   │   ├── VehicleDetails.js   # Детальная информация
│   │   │   └── VehicleForm.js      # Переиспользуемая форма
│   │   └── SpecialOperations/  # Специальные операции
│   ├── services/
│   │   └── api.js              # API сервис (axios)
│   ├── App.js                  # Главный компонент с роутингом
│   ├── App.css                 # Глобальные стили
│   ├── index.js                # Точка входа
│   └── index.css               # Базовые стили
├── package.json
└── README.md
```

## 🔗 API Endpoints

Frontend взаимодействует с backend через следующие endpoints:

### CRUD операции:
- `GET /backend/api/vehicles` - Получить все Vehicle
- `GET /backend/api/vehicles/:id` - Получить Vehicle по ID
- `POST /backend/api/vehicles` - Создать Vehicle
- `PUT /backend/api/vehicles/:id` - Обновить Vehicle
- `DELETE /backend/api/vehicles/:id` - Удалить Vehicle

### Специальные операции:
- `GET /backend/api/vehicles/max-capacity`
- `GET /backend/api/vehicles/by-name-prefix?prefix=xxx`
- `GET /backend/api/vehicles/by-fuel-consumption?minConsumption=xxx`
- `GET /backend/api/vehicles/by-type?type=CAR`
- `POST /backend/api/vehicles/:id/reset-distance`

## 📝 Примеры использования

### Пагинация:
```
GET /backend/api/vehicles?page=0&size=10
```

### Фильтрация:
```
GET /backend/api/vehicles?filterField=name&filterValue=Tesla
```

### Сортировка:
```
GET /backend/api/vehicles?sortField=capacity&sortDirection=desc
```

### Комбинирование:
```
GET /backend/api/vehicles?page=0&size=10&filterField=name&filterValue=Car&sortField=capacity&sortDirection=desc
```

## 🎨 Особенности интерфейса

### Адаптивный дизайн:
- ✅ Работает на десктопе и мобильных устройствах
- ✅ Responsive таблицы и формы

### UX:
- ✅ Информативные сообщения об ошибках
- ✅ Подтверждение удаления
- ✅ Индикаторы загрузки
- ✅ Валидация форм
- ✅ Сортировка по клику на заголовок столбца

### Цветовая схема:
- Первичный цвет: `#667eea` (фиолетовый)
- Успех: `#28a745` (зеленый)
- Ошибка: `#dc3545` (красный)
- Предупреждение: `#ffc107` (желтый)

## 🐛 Отладка

### Проблемы с подключением к backend:

1. Убедитесь, что backend запущен на `http://localhost:8080`
2. Проверьте настройку proxy в `package.json`:
   ```json
   "proxy": "http://localhost:8080"
   ```
3. Проверьте CORS настройки в backend

### Ошибки компиляции:

```bash
# Очистить кэш и переустановить зависимости
rm -rf node_modules package-lock.json
npm install
```

## 📄 Лицензия

ITMO University © 2025

## 👨‍💻 Автор

Information Systems Lab Work

